{
  "name": "jagum-visitor-stats",
  "version": "1.0.0",
  "description": "정책자금 DB 방문자 통계 시스템",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp *.html *.ico *.xml *.txt public/ 2>/dev/null || mkdir public && copy *.html public\\ & copy *.ico public\\ & copy *.xml public\\ & copy *.txt public\\ 2>nul || true"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}